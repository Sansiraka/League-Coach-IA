# Alembic stub configuration. This will be properly initialized using 'alembic init alembic' later, but we create the file to establish the root.
[alembic]
script_location = alembic
sqlalchemy.url = postgresql://coach_user:coach_password@localhost:5432/coach_db
